What is StatefelSet?

A StatefulSet is a Kubernetes resource designed to manage stateful applications. 
Unlike Deployments, StatefulSets maintain sticky identities for Pods, which ensures stable network identities, persistent storage, and ordered deployment/scaling.


Difference between stateful & Stateless application
🔹 Stateful Application
* A stateful application is one that remembers data (state) between user sessions or restarts.
✅ Example:
Databases: MySQL, MongoDB, PostgreSQL
Message Queues: Kafka, RabbitMQ
Other apps: Any app that stores session info, user data, or transactions

🔹 Stateless Application
A stateless application does not store any data (state) locally.
Every request is independent — the app doesn’t remember previous ones.
✅ Example:
Web servers: Nginx, Apache
Frontend apps: React, Angular UI services
API servers: REST APIs that connect to an external database


🔸 In Kubernetes Terms:
| Type          | Object Used             | Storage      | Pod Identity         | Example             |
| ------------- | ----------------------- | ------------ | -------------------- | ------------------- |
| **Stateless** | Deployment / ReplicaSet | No           | Random               | Nginx, Frontend App |
| **Stateful**  | StatefulSet             | Yes (PV/PVC) | Fixed (pod-0, pod-1) | MySQL, MongoDB      |


****>Stateful set always remain a pod name<********

Step 1: create a database service yaml file
vi mysql-service.yaml

apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  clusterIP: None
  selector:
    app: mysql
  ports:
  - name: mysql
    port: 3306


Step 2: create a storage service yaml file
vi storage-class.yml

apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: mysql-storage
provisioner: kubernetes.io/aws-ebs
parameters:
  type: gp2


Step 3: create a yaml file for statefulset
vi mysql-statefulset.yaml

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
spec:
  serviceName: mysql
  replicas: 3
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:5.7
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "password"
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-data
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: mysql-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 5Gi
      storageClassName: mysql-storage


Step 4: run these yaml file mysql-service.yaml, storage-class.yml & mysql-statefulset.yaml

Step 5: now you can verfiy the statefulset
kubectl get statefulset
NAME    READY   AGE
mysql   0/3     3m23s

Step 6: now you can verfiy pod is pending status or not
kubectl get pod
NAME      READY   STATUS    RESTARTS   AGE
mysql-0   0/1     Pending   0          42s
root@ip-172-31-34-75:/home/ubuntu# kubectl get pod
NAME      READY   STATUS    RESTARTS   AGE
mysql-0   0/1     Pending   0          47s
root@ip-172-31-34-75:/home/ubuntu# kubectl get pod
NAME      READY   STATUS    RESTARTS   AGE
mysql-0   0/1     Pending   0          2m31s

Step 7: Check pvc is created or not
kubectl get pvc
NAME                 STATUS    VOLUME   CAPACITY   ACCESS MODES   STORAGECLASS    VOLUMEATTRIBUTESCLASS   AGE
mysql-data-mysql-0   Pending                                      mysql-storage   <unset>                 5m55s
